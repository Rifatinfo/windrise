import { ComingSoon } from "@/components/shared/product-notFound/ComingSoon";

const Notfound = () => {
    // ComingSoon already fills the screen (min-h-screen w-full); no wrapper.
    return <ComingSoon />;
}

export default Notfound;